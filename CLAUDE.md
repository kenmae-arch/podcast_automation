# 鹿島アントラーズ デイリーポッドキャスト

「今日のエピソードを作って」と言われたら、以下の手順でエピソードを1本作成する。
台本はLLM APIではなくClaude Code自身が書く(コスト回避のためのユーザー方針)。

## 手順

1. **リサーチ**: 鹿島アントラーズの直近24時間のニュース・話題を収集する
2. **台本執筆**: `scripts/pending.json` に `{"title", "description", "script"}` のJSONで保存
3. **生成実行**: `python main.py`(venvが必要。requirements.txt参照)
   - Fish Audio(モデル `s2.1-pro-free` 固定)で音声化 → `docs/audio/` に保存
   - `docs/feed.xml` 更新、台本は `scripts/published/` へ自動アーカイブ
4. **確認**: 生成されたMP3をユーザーに届ける

## リサーチのルール(daily-news-automationリポジトリの運用ルール準拠)

- **鮮度が最重要**: 原則として直近24時間以内の情報のみ。各記事の公開日時を必ず確認する。前日に扱った話題の再掲は禁止(新しい進展があるときのみ可)。前日の内容は `scripts/published/` で確認できる。
- **情報源**(優先順):
  - Google News RSS検索「鹿島アントラーズ」(`.env` の `NEWS_FEED_URLS`、`topic_fetcher.py` で取得可)
  - クラブ公式サイト: antlers.co.jp/news/all
  - 公式X: @atlrs_official の当日投稿
  - ドメサカブログ、Yahoo!リアルタイム検索「鹿島アントラーズ」
  - スポーツ紙(報知/日刊/スポニチ/デイリー)のクラブ別ページ
- **移籍報道の網羅**: 「鹿島 移籍」「鹿島 獲得」「鹿島 復帰」「鹿島 加入」等の広いクエリでも検索し、当日の新規スクープを漏らさない。
- **サポーターの話題も扱う**(2026-07-24ユーザーFBで強化): 試合の采配議論、選手のパフォーマンス評、SNSで盛り上がっているネタなど、硬いニュース以外も番組の柱。ニュースが少ない日はファン目線の話題でつなぐ。対象例 — 移籍の噂(例: キム・テヒョン獲得の噂)、新グッズ・新ユニフォーム(例: 26/27 NIKE PLAYERS MODEL)、クラブイベントとその賛否(例: ファミリアフェスタ参加費22,000円への「高すぎ」の声)。噂・反応系は「〜の噂」「Xで話題」など性質が分かる語り方をする。
- **元鹿島の海外組も対象**(2026-07-24ユーザーFBで追加): 佐野海舟(マインツ)、上田綺世(フェイエノールト)など鹿島出身で海外でプレーする選手の移籍・活躍・ゴールを「元鹿島」「(選手名) 最新」等で確認し、動きがあればコーナーとして扱う。
- **毎回の常設エンドコーナー「栄光の軌跡」**(2026-07-28開始→2026-07-29ユーザーFBで常設化): 原則として毎エピソードの最後に、鹿島が獲得した全タイトルを1つ目(1993年サントリーシリーズ制覇。開幕戦・対名古屋でジーコがJリーグ第1号ハットトリック、5対0)から順に、毎回1つずつ紹介する連載コーナーを置く。
  - **ニュースがある日**: まず当日ニュースを紹介し、締めにこのコーナーを短め(番組後半の1ブロック)に付ける。
  - **ニュースが乏しい日**: このコーナーを主役に据えてエピソードを構成してよい(2026-07-29の#1がこの形)。
  - 連載の続き番号・次に扱うタイトルは `scripts/published/` の過去回で必ず確認する(重複させない)。#1=1993年1stステージ→#2=1996年Jリーグ年間制覇(クラブ初の年間王者)…と史実順に進める。史実は必ず裏取りする。ネタが尽きたら名選手・名勝負など他の振り返りに広げてよい。
- **事実確認**: 記事のog:descriptionや本文で裏付けを取ってから台本に書く。憶測を事実のように語らない。移籍の噂は「〜と報じられています」と出典を明示する。

## 台本のトーン

- サポーター仲間に話しかける、親しみのある「です・ます」調の1人語り
- 目安1200〜2000文字(音声3〜5分)
- 読み上げ用テキストのみ(見出し・記号・効果音指示は入れない)
- クロージングは次の試合日程や「スタジアムで会いましょう」系の一言で締める

## 読み仮名辞書(pronunciation_dict.json)

- TTSが読み間違える固有名詞(選手名・地名など)は `pronunciation_dict.json` に「表記→読み仮名」で登録する。音声生成時に自動置換される(台本JSON自体は漢字表記のままでよい)。
- ユーザーから読み間違いの指摘があったら、必ずこの辞書に追記すること。
- 新しい選手名・監督名を台本に使うときは、読みが自明でなければ辞書に登録してから生成する。

## 技術メモ

- 実行環境: scratchpadのvenv(なければ `python3 -m venv` で作成して `pip install -r requirements.txt`)
- feedparserは直接URLを渡すとSSLエラーになる環境があるため、`topic_fetcher.py` はrequests経由で取得している
- ローカルにffmpegがないため、複数チャンクの結合はバイト連結フォールバックで動く
